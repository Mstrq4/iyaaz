# Access modes

IYAAZ supports three deployment modes from one codebase: public, private employee access, and shared limited links. Private/shared authorization is enforced before protected content delivery using stateless HMAC-signed credentials with expiry, not by merely hiding UI controls.
