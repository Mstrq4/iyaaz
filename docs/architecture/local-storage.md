# Browser-local storage

`sessionStorage` holds in-progress prompt form state. `localStorage` holds favorites, history, client profiles, theme/language preferences, and other explicitly persistent browser-local settings. No client profile or prompt content is persisted to the server by IYAAZ.
