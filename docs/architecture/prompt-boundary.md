# Prompt execution boundary

IYAAZ prepares deterministic prompt text for the user. It does not send prompts, images, or client data to an AI model. Where a shortcut requires a reference image or logo, the UI displays an attachment reminder for use after copying the prompt; there is no file-upload component in IYAAZ.
