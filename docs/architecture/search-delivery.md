# Search delivery

The library contains 5,812 unique shortcuts. The web runtime uses a sanitized compressed snapshot and server-side loading/search helpers so the initial client bundle does not need to embed the full workbook or its source/reference fields. Search normalizes Arabic text, supports English case-insensitive matching, and combines query matching with domain/category/subcategory/type filters and explicit sorting.
