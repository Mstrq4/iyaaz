# Remote quality gates

The final review candidate must pass GitHub Actions for install, lint, type checking, build, logic tests, Python validation, and Playwright browser E2E. The final candidate SHA in the pull request must match the SHA used by the successful checks and any preview verification.
