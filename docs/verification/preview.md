# Preview verification

A non-production preview is verified, when available, against the same final candidate SHA used by CI and browser E2E. Production remains separately gated by explicit user approval.
